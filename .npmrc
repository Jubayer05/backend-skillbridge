# Hoist all @types/* packages so TypeScript can resolve sub-dependencies
# (e.g. @types/express-serve-static-core) regardless of pnpm version on Vercel.
public-hoist-pattern[]=@types/*
public-hoist-pattern[]=*types*
